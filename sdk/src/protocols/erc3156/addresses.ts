import type { ChainDeployment } from "../../core/types.js";

// ERC-3156 is a standard interface, not a single deployment.
// Addresses below are well-known lenders that implement the interface.
export const deployments: ChainDeployment[] = [];
